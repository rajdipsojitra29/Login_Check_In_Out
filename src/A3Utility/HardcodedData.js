// HardcodedData.js
import {
    Dimensions,
} from 'react-native';
import Toast from 'react-native-root-toast'











class HardcodedData {
    static app() {
        // Toast.show('string',{position:Toast.positions.CENTER})
    }

    static login(login) {
        // console.log('login')
        // Toast.show('login',{position:Toast.positions.CENTER})
    }

    static checkInOut(checkInOut) {
        // console.log('checkInOut')
        // Toast.show('checkInOut',{position:Toast.positions.CENTER})
    }
}


export default(HardcodedData);